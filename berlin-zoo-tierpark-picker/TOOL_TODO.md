# Berlin Zoo Tierpark Picker - BerlinTools TODO

Created locally on 2026-06-20 for the Wix Blog draft `zoo-berlin-vs-tierpark-berlin`.

## Current state

- Widget source exists at `berlin-zoo-tierpark-picker/index.html`.
- Local desktop/mobile QA passed with horizontal overflow 0.
- Wix Blog draft embeds `https://fenerszymanski.github.io/berlinwalk-widgets/berlin-zoo-tierpark-picker/?v=20260620a`.
- GitHub Pages returns 404 until the widget repo is pushed/deployed.

## Blocked before BerlinTools completion

Do not add this tool to `tools-hub/data.json` or create the live `BerlinTools` CMS item until a dedicated glossy ChatGPT-browser icon is generated, processed to canonical `512` and `160` PNGs, uploaded to Wix Media, and wired into:

- `tools-home/icons/<slug>.png`
- `tools-home/icons/<slug>-160.png`
- icon manifests/upload cache
- `tools-hub/data.json` `image`
- BerlinTools CMS `iconUrl`
- BerlinTools layout icon map custom embed

## Icon prompt draft

Create a glossy 3D BerlinWalk app icon in the existing BerlinTools family: square green glass tile, yellow circular medallion, no text, no letters, no numbers. Central object: a split-choice animal park symbol, with a small stylized elephant-gate arch on one side and a leafy palace-park path on the other, plus a simple animal silhouette. Warm BerlinWalk green/yellow/lime palette, polished 3D depth, clean transparent or cream outer corners, readable at 64px.

