# Daily-blog tool icon - 2026-07-26 (berlin-pass-cost-comparison)

For the "Berlin Pass Cost Comparison" tool (blog:
`/post/which-berlin-pass-is-worth-it`).

Non-paid route only: built-in image generation first, else Yusuf's logged-in
ChatGPT web via Chrome. Same fixed 3-layer BerlinTools family as
`daily-blog-icons-20260715-prompts.md`. Motif deliberately avoids a ticket
MACHINE (owned by `berlin-ticket-machine-simulator`), a clock, a map pin, a
döner and a moon. Instead: a single transit pass card + a gold coin =
"which pass is worth the money".

Canonical crops after generation: `tools-home/icons/berlin-pass-cost-comparison.png`
(512) and `berlin-pass-cost-comparison-160.png` (160), RGBA, cream/transparent
corners, no text.

## `berlin-pass-cost-comparison` (Berlin Pass Cost Comparison)

```text
Create ONE square 1:1 app icon for BerlinWalk, in a strict fixed style. STRUCTURE, exactly three layers and nothing else: (1) a plain cream background; (2) a solid deep green #1B5E20 rounded-square app tile with soft glossy 3D bevel that fills the frame; (3) one large centered bright yellow #FFE600 glossy circular medallion disc on the tile. Resting on the yellow disc is ONE clear glossy soft-3D emblem: a single deep green #1B5E20 glossy transit pass card shaped like a rounded rectangle ticket tilted slightly, with one small punched notch cut into its left edge, and a chunky warm-gold glossy round coin overlapping its lower-right corner, so it reads instantly as choosing a travel pass by cost. The pass card is solid deep green with soft glossy highlights; the coin is rich warm gold #F2B01E with soft glossy highlights and a plain smooth face. No text, no letters, no numbers, no currency symbols, no logos, no barcode, no machine, no map, no clock, no people, no second disc, no third object beyond the single card and the single coin. Do NOT use a white, glass, frosted, or pale tile. Chunky simple shapes, high contrast, centered, readable at 160x160, clean.
```
