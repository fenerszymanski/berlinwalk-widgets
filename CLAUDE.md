# Claude Code — Berlin Walk

Read `AGENTS.md` for full project context (architecture, live endpoints, conventions, brand system, open items, and protocols). This file just imports it.

@AGENTS.md

## Quick orientation

At the start of every session, also skim the most recent entry in `SESSION_LOG.md` — that tells you what the previous agent (Claude Code or Codex) just did and what's most urgent. At the end of every session, append a new entry there.

The protocol for session log entries is documented in §9 of `AGENTS.md`.
